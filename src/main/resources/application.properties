# src/main/resources/application.properties
# Set application port to avoid 8080 conflict
server.port=8081

# Optional local JWT secret for demo; override via env var ONBOARDING_JWT_SECRET in real runs
onboarding.jwt.secret=${ONBOARDING_JWT_SECRET:change-me-for-local-demo}

# Optional: enable a local profile
spring.profiles.active=local

